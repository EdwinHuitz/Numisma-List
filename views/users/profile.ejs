<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= title %></title>
    <link rel="stylesheet" href="../stylesheets/style.css" /><link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
</head>
<%- include("../partials/header") %>


<h1><%= (user._id.toString()===orig._id.toString())?'My Profile':title+`'s Profile` %></h1>
<% if(user._id.toString()===orig._id.toString()){ %>
    <div style="width:80vw;position:absolute;left:10vw;display:flex;justify-content: space-between;
    background-color:rgb(84, 145, 165)!important;"><button href="#"
    onclick="let list = document.getElementById('newlist').style;if(list.display==='block'){list.display='none'}else{list.display='block'}" class="navbtn">New
    List</button><b style="color:white!important;">|</b>
    <button href="#"
    onclick="let list = document.getElementById('mylists').style;if(list.display==='block'){list.display='none'}else{list.display='block'}"class="navbtn">My
    Lists</button><b style="color:white!important;">|</b>
    <button href="#"
    onclick="let coin = document.getElementById('addcoin').style;if(coin.display==='block'){coin.display='none'}else{coin.display='block'}"class="navbtn">Add
    a Coin</button><b style="color:white!important;">|</b>
    <button href="#"
    onclick="let coin = document.getElementById('mycoins').style;if(coin.display==='block'){coin.display='none'}else{coin.display='block'}"class="navbtn">My Coins</button></div>
    <div style="width:100vw;padding-top: 8vh;">
    <div id="newlist" style="display:none;"><%- include("../partials/list") %></div>
    <div id="mylists" style="display:none;"><%- include("../partials/mylist") %></div>
    <div id="addcoin" style="display:none;"><%- include("../partials/coin") %></div>
    <div id="mycoins" style="display:none;"><%- include("../partials/mycoin") %></div>
    </div>
<%}else{%>
    <div class="card">
        <div id="mylists" style="display:block;"><%- include("../partials/mylist") %></div>
    </div>
    <div class="card">
        <div id="mycoins" style="display:block;"><%- include("../partials/mycoin") %></div>
    </div>
<%}%>

<%- include("../partials/footer")%>